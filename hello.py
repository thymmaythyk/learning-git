print("hello squidward")

# git init will initialize a repository (makes a folder to work in)
# git status gives us information about the files (tracked, modified, untracked)
# git add (for staging/organizing stuff)
# git commit -m "" (uesd to take a snapshot of the project of that time)
            # ^ message


# git clone ____ (repo/url)
# git push takes local commits and pushes it to another repo
# git pull takes latest changest and pull from repo to local workspace